# Published Content

This folder is reserved for reviewed content that is ready for publication or release packaging.

It is not automatically consumed by the app in the current MVP. Runtime content still comes from `app/assets/content` unless a future task changes that pipeline.
